package app.brucehsieh.logneko.presentation.modal

/**
 * Represents whether the log viewer is showing items via paging or directly from an in-memory list.
 */
enum class LineSource {
    PAGING,
    FULL_LIST
}
